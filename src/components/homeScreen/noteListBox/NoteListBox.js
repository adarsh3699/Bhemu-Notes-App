import React from 'react';
import { NoteListBoxStyle } from './NoteListBoxStyle';

import { View, Text } from 'react-native';

const NoteListBox = () => {
	return (
		<View>
			<Text>This is the NoteListBox component.</Text>
		</View>
	);
};

export default NoteListBox;
